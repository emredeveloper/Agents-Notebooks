# LM Studio Configuration
# For local development, use: http://localhost:1234
# For ngrok tunnel, use your ngrok URL (e.g., https://xxxxx.ngrok-free.dev)
NGROK_URL=http://localhost:1234

# Optional: API key (not needed for LM Studio local server)
# API_KEY=not-needed
